package http

import (
	"github.com/xiaofan193/xifancloud193/internal/framework"
)

func Route(r *framework.GinEngine) {
	//container := r.GetContaier()
	//configService := container.MustMake(contract.ConfigKey).(contract.Config)

	// todo Path first /Search for files in the dist the service provider
	//r.Use(st)
	// todo swagger
	// daynamic route define
	//_ = demo.Register(r)

}
